
import PomodoroTimer from "././components/PomodoroTimer";

export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-gradient-to-r from-red-400 to-yellow-400">
      <PomodoroTimer />
    </main>
  );
}
